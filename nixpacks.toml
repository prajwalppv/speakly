[phases.setup]
nixPkgs = ["python311", "postgresql", "uv"]

[phases.build]
cmds = [
  "cd backend && uv sync --frozen --no-dev"
]

[start]
cmd = "cd backend && uv run uvicorn app.main:app --host 0.0.0.0 --port $PORT"

[release]
cmd = "cd backend && uv run alembic upgrade head"
